import React from "react";

export const LanguageContext=React.createContext(); //here you are the catastrophy !!!!!!!1 the name of the context and the provider should start with capital
export const LanguageProvider=LanguageContext.Provider;